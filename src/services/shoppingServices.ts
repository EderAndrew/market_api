import { collection, doc, getDocs, query, setDoc, where } from "firebase/firestore/lite"
import { db } from "../firebase/config"

const ShoppingServices = {
    createShopping: async (name: string) => {
        try{

        }catch(err){
            
        }
    }
}